import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "The 7-Stage Process to Book 3+ Meetings Every Single Week",
  description:
    "Stop guessing. Start converting. Our proven 7-stage framework turns prospects into meetings. Boost response rates from 10% to 40-60% and book more high-quality meetings.",
};

export default function WinWithDMsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

